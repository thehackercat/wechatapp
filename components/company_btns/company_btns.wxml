<view class="company-handle {{contactWho==='both'?'fixed':''}}">
    <button bindtap='contactCompany' wx:if="{{contactWho!=='officer'}}">联系公司</button>
    <button bindtap='contactOfficer' wx:if="{{contactWho!=='company'}}">联系资本小组</button>
    <button bindtap='bindViewProfile' wx:if="{{contactWho!=='both'}}">查看详细资料</button>
</view>